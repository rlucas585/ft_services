apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    isDefault: true
    access: proxy
    database: $INFLUX_DATABASE
    user: $INFLUX_USER
    url: $INFLUX_URL
    jsonData:
      httpMode: GET
    secureJsonData:
      password: $INFLUX_PASS
